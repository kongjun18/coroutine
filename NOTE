struct schedule 代表全局协程调度器，其中包含共享栈、协程列表和上一个协程 context。任意时刻最多有一个协程在运行，使用共享栈。
resume 时保存当前 context，切换到目标协程的 context，拷贝目标协程私有栈到共享栈。
yield 时拷贝共享栈（存放当前协程的栈数据）到协程私有栈，然后切换到上一个协程 context。
第一次 resume 时没有上一次协程 context，从当前上下文构造 context，并让协程连接到它。
协程私有栈的分配是延迟分配，在第一次 yield 时分配（私有栈容量不足，扩容，即相当于分配）。
